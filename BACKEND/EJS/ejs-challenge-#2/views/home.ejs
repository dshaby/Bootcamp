<%- include('partials/header'); %>
    <h1>Welcome!</h1>
    <p>
        <%= ejsHomeStartingContent%>
    </p>

    <% ejsPosts.forEach((post)=> { %>
        <h3>
            <%= post.title %>
        </h3>
        <p>
            <%= post.body %>
        </p>

        <% }); %>

            <%- include('partials/footer'); %>